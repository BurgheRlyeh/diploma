#pragma once

#include "framework.h"

//HRESULT loadShaderBlob(const std::wstring& filename, ID3DBlob** pBlob) {
//    std::wstring filepath{ getOutDir() + L"\\" + filename };
//    return D3DReadFileToBlob(filepath.c_str(), pBlob);
//}
